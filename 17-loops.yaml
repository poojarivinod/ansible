- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" # item is a reserved word in ansible for to get list of things
    loop:
    - vinod 
    - suda
    - vishnu
